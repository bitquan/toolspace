# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
  workflow_run:
    workflows: [ "🚀 PR CI (Lean & Fast)" ]
    types: [ completed ]
    branches: [ main ]

jobs:
  check_pr_ci:
    name: Check PR CI Passed
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success' || github.event_name ==
      'push'
    steps:
      - name: ✅ PR CI Passed
        run: echo "PR CI checks passed, proceeding with deployment"

  build_and_deploy:
    runs-on: ubuntu-latest
    needs: [ check_pr_ci ]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.24.3"
          channel: "stable"

      - name: Enable Flutter web
        run: flutter config --enable-web

      - name: Get dependencies
        run: flutter pub get

      - name: Install Node dependencies
        run: npm ci

      - name: Build web app
        run: flutter build web --release
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOOLSPACE_BETA }}
          channelId: live
          projectId: toolspace-beta
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
permissions:
  contents: read
  actions: read
